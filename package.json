{
  "name": "pill-reminder",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build --compile --target=bun-linux-x64 ./src/index.ts --outfile pill"
  },
  "dependencies": {
    "@larksuiteoapi/node-sdk": "^1.55.0",
    "await-to-js": "^3.0.0",
    "croner": "^9.1.0",
    "drizzle-orm": "^0.45.1",
    "mitt": "^3.0.1",
    "pino": "^10.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.8"
  }
}
